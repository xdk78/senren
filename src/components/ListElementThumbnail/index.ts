import styled from 'utils/styled-components'

const ListElementThumbnail = styled.img`
  object-fit: cover;
  border-radius: 8px;
  width: 85px;
  height: 85px;
  object-fit: cover;
`
export default ListElementThumbnail
